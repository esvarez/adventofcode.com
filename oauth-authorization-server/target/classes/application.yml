server:
  port: 8083

spring:
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb

  h2:
    console:
      path: "/h2"
      enabled: true

keycloak:
  server:
    contextPath: /auth
    adminUser:
      username: bael-admin
      password: pass
    realmImportFile: baeldung-realm.json
